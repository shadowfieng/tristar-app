import { Flex } from 'antd'
import { SearchMovieForm } from '../../components'

export const Home = () => {
  return (
    <Flex>
      <SearchMovieForm onSubmit={() => {}} />
      {/* MOVIES */}
    </Flex>
  )
}
